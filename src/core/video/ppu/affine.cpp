#include "common/logger.h"
#include "core/video/ppu/ppu.h"

namespace core {

void PPU::render_affine(int bg, int line) {
    logger.todo("PPU: handle affine rendering");
}

void PPU::render_extended(int bg, int line) {
    logger.todo("PPU: handle extended rendering");
}

void PPU::render_large(int bg, int line) {
    logger.todo("PPU: handle large rendering");
}

} // namespace core